import React from 'react';
import { Typography } from '@mui/material';

function ResetPassword() {
  return <Typography variant="h4">Reset Password Page</Typography>;
}

export default ResetPassword;
