import React from 'react';
import { Typography } from '@mui/material';

const UsersList = () => (
  <Typography variant="h5">Users List (placeholder)</Typography>
);

export default UsersList;
